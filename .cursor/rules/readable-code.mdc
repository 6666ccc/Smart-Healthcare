---
description: 
alwaysApply: true
---

# 可读性优先

写代码时默认按「团队里有 Python 新人也能直接读懂」的标准，不要追求行数最少或写法「地道」。

## 通用原则

- 逻辑拆成清晰步骤，用注释标出「第几步 / 做什么」
- 复杂条件拆成多行 `if/else`，不要堆在一行
- 一个函数只做一件事；超过 ~15 行考虑拆成小函数
- 变量名说清含义（如 `raw_interrupts`、`last_message`），避免 `x`、`v`、`tmp`
- 只在确实简化阅读时使用语法糖；新人需要查文档才能懂的写法，优先不用

## Python：避免 vs 推荐

```python
# ❌ 紧凑写法（新人难读）
interrupts = []
for item in getattr(result, "interrupts", ()) or ():
    value = getattr(item, "value", item)
    if isinstance(value, dict):
        interrupts.append(value)
messages = value.get("messages", []) if isinstance(value, dict) else []
content = getattr(final_msg, "content", "") or "" if final_msg else ""

# ✅ 易读写法
raw_interrupts = getattr(result, "interrupts", None)
if raw_interrupts is None:
    raw_interrupts = []

interrupts = []
for item in raw_interrupts:
    if hasattr(item, "value"):
        value = item.value
    else:
        value = item
    if isinstance(value, dict):
        interrupts.append(value)

messages = []
if isinstance(value, dict):
    messages = value.get("messages", [])
```

## Python：具体避免

- 链式 `getattr(... ) or ...` — 拆成 `hasattr` + 显式分支，或先赋默认值再处理
- 一行三元 `a if cond else b` 嵌套 — 改成多行 `if/else`
- `if items:` 表「非空」— 新人场景可用 `len(items) > 0`（简单布尔 guard 除外）
- 魔法负数索引 — 可写 `messages[len(messages) - 1]` 或先 `last = messages[-1]` 并命名
- 过度列表推导 / 嵌套 lambda — 用普通 `for` 循环

## 何时可以写紧凑

- 项目里已有相同模式的文件，且只是小改动需保持一致
- 用户明确要求简洁/性能/与库惯用法对齐

默认仍选易读；紧凑写法需能一句话说清「为什么这里例外」。
