---
description: 注释与文档字符串要写清楚「是什么、从哪来、变成什么」，避免缩写式黑话
alwaysApply: false
---

# 注释要详细、说人话

写注释、docstring、函数名旁说明时，默认读者是**没看过框架文档的新人**。注释要回答三件事：**输入是什么形态、这一步做什么、输出是什么形态**。

## 避免 vs 推荐

```python
# ❌ 缩写式黑话（读者不知道「中断字典」指什么）
def _extract_interrupt_dicts(result) -> list[dict]:
    """提取中断字典列表。"""

# ✅ 说清楚来源、动作、结果
def _extract_interrupt_dicts(result) -> list[dict]:
    """把 LangGraph / HITL 返回的 Interrupt 对象解包为普通 dict 列表。

    输入：TOOL_AGENT.invoke(version="v2") 的 GraphOutput，可能有 .interrupts
    输出：[{"action_requests": [...], "review_configs": [...]}, ...]
    供 _format_interrupts 转成前端可读的 title / summary / details。
    """
```

## 具体原则

- **函数 docstring**：写清输入类型/来源、处理步骤、输出用途；不要只重复函数名
- **变量注释**：说明「这个变量里装的是什么」，如 `raw_interrupts` 优于 `items`
- **步骤注释**：写「第 N 步：做什么 + 为什么」，不要只写「处理数据」
- **专有名词**：首次出现可带括号解释，如 `GraphOutput（Agent invoke 的 v2 返回值）`
- **禁止**：用 2～4 个字的概括代替说明（如「格式化」「提取列表」「处理结果」）

## 与可读性规则的关系

`readable-code.mdc` 管代码结构；本规则管**注释是否足够让人读懂结构和数据流**。两者同时生效。
