---
description: Debug 模式 — 只读、逐步追踪调用链，不修改代码
alwaysApply: true
---

# Debug 模式

当用户明确说「进入 debug 模式」或类似表述时，立即切换为 Debug 工作方式，并持续到用户说退出 debug 模式为止。

## 只读，不改代码

- 默认等同 **Ask 模式**：只读分析、解释、讨论，**不修改任何文件**
- 即使用户未开启 Ask 模式，也按 Ask 模式约束自己
- 不主动 `Write` / `StrReplace` / 运行会改仓库的命令；用户要改代码时先确认是否退出 debug 模式

## 调用链：只给下一步

用户按调用链逐步排查时，**每次只回答当前节点的下一步**，不要一次性列出整条链路。

示例：链路 `a → b → c → d → e → f → g`

| 用户当前位置 | 你只回答 |
|-------------|---------|
| 在 a | 下一步是 **b** |
| 在 b | 下一步是 **c** |
| 在 c | 下一步是 **d** |

- 不要提前剧透 c、d、e…  unless 用户明确要求看完整链路
- 若用户尚未说明当前在哪一步，先问「你现在停在哪一步？」再答下一步

## 讨论范围

确认下一步后，讨论集中在 **当前步与下一步之间** 的代码：

- 参数如何传递、谁调用谁、返回值形态
- 该段内的分支、异常、状态变化
- 用户贴日志/断点信息时，结合这段代码解释

不要跳到更下游的步骤，除非用户已走到那一步并询问。

## 代码引用：标注精确位置

解释代码时必须附带 **文件路径 + 行号范围**，用 `file:line–line` 或 `file:line` 格式：

- 引用函数/方法：使用定义行范围，如 `registration_agent（nodes.py:332-371）`
- 引用代码段：使用起止行，如 `config（nodes.py:338-341）`
- 引用单行：使用单行号，如 `中断守卫（nodes.py:102）`

**为什么：** 方便用户直接在 IDE 中点击跳转，定位到精确代码位置。

**示例：**
| ❌ 不推荐 | ✅ 推荐 |
|-----------|--------|
| config 参数传入了 thread_id | `config（nodes.py:338-341）` 传入了 thread_id |
| store_memory 里有守卫逻辑 | `store_memory（nodes.py:102）` 有守卫逻辑 |
| _extract_interrupt_dicts 函数 | `_extract_interrupt_dicts（nodes.py:311-330）` |

## 退出

用户说「退出 debug 模式」或开始要求改代码/实现功能时，结束 Debug 约束，恢复正常 Agent 行为。
