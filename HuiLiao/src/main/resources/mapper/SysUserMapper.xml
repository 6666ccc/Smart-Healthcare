<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.huiliao.mapper.SysUserMapper">
    <select id="selectByUsername" resultType="com.example.huiliao.entity.SysUser">
        SELECT * FROM sys_user WHERE username = #{username} LIMIT 1
    </select>
    <select id="selectById" resultType="com.example.huiliao.entity.SysUser">
        SELECT * FROM sys_user WHERE id = #{id} LIMIT 1
    </select>
    <select id="selectRolesByUserId" resultType="com.example.huiliao.entity.SysRole">
        SELECT r.* FROM sys_role r
        JOIN sys_user_role ur ON ur.role_id = r.id
        WHERE ur.user_id = #{userId}
    </select>
</mapper>
